apiVersion: apps/v1
kind: Deployment
metadata:
  name: rabbitmq-ex4
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rabbitmq-ex4
  template:
    metadata:
      labels:
        app: rabbitmq-ex4
    spec:
      containers:
        - name: rabbitmq
          image: rabbitmq:3-management
          imagePullPolicy: IfNotPresent
          env:
            - name: RABBITMQ_DEFAULT_USER
              valueFrom:
                secretKeyRef:
                  name: rabbit-credentials-ex4
                  key: RABBITMQ_USER
            - name: RABBITMQ_DEFAULT_PASS
              valueFrom:
                secretKeyRef:
                  name: rabbit-credentials-ex4
                  key: RABBITMQ_PASS
          ports:
            - name: amqp
              containerPort: 5672
            - name: management
              containerPort: 15672
---
apiVersion: v1
kind: Service
metadata:
  name: rabbitmq-ex4
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: rabbitmq-ex4
  ports:
    - name: amqp
      port: 5672
      targetPort: 5672
    - name: management
      port: 15672
      targetPort: 15672
